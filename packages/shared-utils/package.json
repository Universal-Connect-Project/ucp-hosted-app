{
  "name": "@repo/shared-utils",
  "version": "0.0.1",
  "description": "Shared utils for the universal connect project hosted apps",
  "main": "dist/src/index.js",
  "scripts": {
    "build": "npm run clean && npx tsc -b",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
