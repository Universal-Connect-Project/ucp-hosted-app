describe("performance auth rate limiting", () => {
  it("allows more requests than the default limit if provided with proper authorization", () => {});
  it("rate limits quickly if the user is not authorized", () => {});
});
