{
  "name": "ucp-app",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "prettier": "turbo run prettier",
    "test": "turbo run test",
    "prepare": "husky install"
  },
  "devDependencies": {
    "husky": "^9.0.11",
    "turbo": "^1.13.3"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "npm@10.5.0",
  "workspaces": [
    "apps/*"
  ]
}
